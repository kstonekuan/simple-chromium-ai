{
	"name": "demo-extension",
	"version": "1.0.0",
	"description": "Minimal Chrome extension demonstrating Chrome's built-in AI API.",
	"main": "popup.js",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"lint": "biome check --write .",
		"typecheck": "tsc --noEmit",
		"check": "pnpm run lint && pnpm run typecheck",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"type": "module",
	"dependencies": {
		"simple-chromium-ai": "0.1.4"
	},
	"devDependencies": {
		"@crxjs/vite-plugin": "2.4.0",
		"@types/node": "25.5.0",
		"typescript": "6.0.2",
		"vite": "8.0.5"
	}
}
